1 task
select plaintext from wordform limit 10;

2 task
select plaintext from wordform where plaintext ilike 'a%';

3 task
select title, genretype from work where genretype = 'p';

4 task
select avg(totalparagraphs) from work where genretype = 't';

5 task
select title from work where totalwords > (select avg(totalwords)from work);

6 task
select character.charname, character.speechcount, work.title from character left join character_work on character.charid = character_work.charid left join work on character_work.workid = work.workid;

7 task
select round(avg(speechcount)) from character join character_work on character.charid = character_work.charid join work on character_work.workid = work.workid and work.title = 'Romeo and Juliet';

8 task
select  section, sum(wordcount) from paragraph group by section;

9 task
select charname, speechcount from character where speechcount >14 and speechcount <31;

10 task
select title, year from work where year > 1600 and year <1700;

11 task
select longtitle from work where title like '%the%';

12 task
select distinct section from paragraph;

13 task
select c.chapterid, c.description, w.title from chapter as c join work as w on c.workid = w.workid;

14 task
select p.paragraphnum, c.charname, c.speechcount from paragraph as p join character as c on p.charid = c.charid;

15 task
select p.paragraphnum, w.title, w.year from paragraph as p join work as w on p.workid = w.workid;
